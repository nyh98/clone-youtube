import React from 'react';

export default function SubscribePage() {
  return (
    <div className="w-full h-full flex flex-col  justify-center items-center">
      <p>
        죄송합니다, 현재 홈페이지는 아직 로그인 기능이 없어 구독 리스트를 불러올
        수 없습니다
      </p>
      <p>
        Sorry, the current homepage does not yet have a login function, so we
        cannot load the subscription list. You can not
      </p>
    </div>
  );
}
